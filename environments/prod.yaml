cloud: ""       # preenchido via sync do GitHub Projects
region: ""

app:
  port: 3000
  replicas: 2
  memory: 512Mi
  cpu: 250m

image:
  registry: ghcr.io/muri1o
  tag: ""       # preenchido pela pipeline com tag semântica

env:
  NODE_ENV: production
  LOG_LEVEL: info

synthetic_tests:
  - path: /health
    expected_status: 200
    max_response_ms: 500
