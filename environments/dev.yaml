cloud: ""       # preenchido via sync do GitHub Projects
region: ""

app:
  port: 3000
  replicas: 1
  memory: 512Mi
  cpu: 250m

image:
  registry: ghcr.io/muri1o
  tag: ""       # preenchido pela pipeline com SHA do commit

env:
  NODE_ENV: development
  LOG_LEVEL: debug

synthetic_tests:
  - path: /health
    expected_status: 200
    max_response_ms: 500
